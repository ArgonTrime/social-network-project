

let initialState = {
    friend: [
        {id: 1, name: 'Ihar'},
        {id: 2, name: 'Julia'},
        {id: 2, name: 'Marina'}
    ]
};

const navBarReducer = (state = initialState, action) => {
    return state;
};

export default navBarReducer;